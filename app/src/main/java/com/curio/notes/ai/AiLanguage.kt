package com.curio.notes.ai

/** Language the AI must answer in. The JSON schema and type enum
 * never change — only the human-readable values are translated. */
enum class AiLanguage {
    ENGLISH,
    SPANISH
}
